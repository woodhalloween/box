# 顔認識統合システム用追加依存関係

# 顔認識・検出ライブラリ
face-recognition>=1.3.0
mediapipe>=0.10.0
dlib>=19.24.0  # face-recognitionの依存関係

# 科学計算・データ処理
scikit-learn>=1.0.0
scipy>=1.9.0

# データベース（標準ライブラリのため不要だが明記）
# sqlite3  # 標準ライブラリ

# YAML設定ファイル処理
PyYAML>=6.0

# 画像処理（既存に追加）
Pillow>=9.0.0

# ログ・デバッグ用
colorlog>=6.0.0

# オプション：GPU対応（dlib）
cmake>=3.22.0  # dlibのコンパイルに必要（オプション）

# パフォーマンス向上
numba>=0.56.0  # 計算高速化（オプション） 